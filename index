<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Saqr Almeliki — CV</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg: #0f1724;
      --card: linear-gradient(135deg,#0ea5a4 0%, #7c3aed 100%);
      --muted: rgba(255,255,255,0.75);
      --glass: rgba(255,255,255,0.06);
    }
    *{box-sizing:border-box}
    body{
      margin:0;font-family:Poppins,system-ui,-apple-system,Segoe UI,Roboto,Arial;
      background: radial-gradient(1200px 600px at 10% 10%, rgba(124,58,237,0.12), transparent),
                  radial-gradient(1000px 500px at 90% 90%, rgba(14,165,164,0.08), transparent),
                  #071024;color:#fff;line-height:1.45;
      -webkit-font-smoothing:antialiased;-moz-osx-font-smoothing:grayscale;
      padding:48px;
      }
    .container{max-width:980px;margin:0 auto;display:grid;grid-template-columns:320px 1fr;gap:28px;align-items:start}

    /* left */
    .card{
      background:linear-gradient(180deg, rgba(255,255,255,0.03), rgba(255,255,255,0.02));
      border-radius:16px;padding:22px;backdrop-filter: blur(6px);box-shadow:0 6px 30px rgba(2,6,23,0.6);
    }
    .profile{
      display:flex;flex-direction:column;align-items:center;text-align:center;gap:14px
    }
    .avatar{
      width:140px;height:140px;border-radius:20px;overflow:hidden;border:3px solid rgba(255,255,255,0.06);
      background:linear-gradient(135deg,#111827,#0b1220);display:flex;align-items:center;justify-content:center
    }
    .avatar svg{width:80px;height:80px;opacity:0.9}
    h1{margin:0;font-size:20px;font-weight:700;letter-spacing:0.2px}
    p.role{margin:0;color:var(--muted);font-weight:600}
    .contact{margin-top:12px;font-size:14px;color:var(--muted);display:flex;flex-direction:column;gap:8px}
    .chip{display:inline-flex;gap:10px;align-items:center;padding:6px 10px;border-radius:999px;background:var(--glass);font-weight:600}

    /* skills */
    .section{margin-top:18px}
    .section h3{margin:0 0 10px 0;font-size:13px;color:#e9eef8}
    .skills{display:flex;flex-wrap:wrap;gap:8px}
    .skill{padding:8px 12px;border-radius:12px;background:rgba(255,255,255,0.03);font-weight:600;font-size:13px}

    /* right */
    .main{
      background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      border-radius:16px;padding:26px;box-shadow:0 6px 30px rgba(2,6,23,0.6);
    }
    .headline{display:flex;justify-content:space-between;align-items:flex-start;gap:12px}
    .about{color:var(--muted);font-size:15px}

    .row{display:flex;gap:18px;margin-top:18px}
    .col{flex:1}
    .info-block{padding:14px;border-radius:12px;background:linear-gradient(90deg, rgba(255,255,255,0.01), rgba(255,255,255,0.02));}
    .info-block h4{margin:0 0 8px 0;font-size:14px}
    .info-block p{margin:0;color:var(--muted);font-size:14px}

    .projects{display:grid;gap:12px}
    .project{display:flex;justify-content:space-between;align-items:center;padding:12px;border-radius:10px;background:rgba(255,255,255,0.02)}
    .project .meta{display:flex;flex-direction:column}

    .download{margin-top:18px;display:inline-flex;gap:10px;align-items:center}
    .btn{cursor:pointer;border:0;padding:10px 14px;border-radius:10px;font-weight:700;background:var(--card);color:#fff}
    .ghost{background:transparent;border:1px solid rgba(255,255,255,0.06);color:var(--muted);padding:9px 12px;border-radius:10px}

    footer{margin-top:18px;color:var(--muted);font-size:13px;text-align:center}

    /* responsive */
    @media (max-width:880px){
      .container{grid-template-columns:1fr; padding:20px}
      .avatar{width:110px;height:110px}
    }

    /* print */
    @media print{
      body{background:white;color:black;padding:0}
      .container{box-shadow:none}
      .btn,.ghost{display:none}
    }
  </style>
</head>
<body>
  <div class="container">
    <aside class="card">
      <div class="profile">
        <div class="avatar">
          <!-- simple SVG placeholder -->
          <svg viewBox="0 0 100 100" fill="none" xmlns="http://www.w3.org/2000/svg">
            <rect width="100" height="100" rx="14" fill="url(#g)"/>
            <defs>
              <linearGradient id="g" x1="0" x2="1">
                <stop offset="0" stop-color="#7c3aed"/>
                <stop offset="1" stop-color="#0ea5a4"/>
              </linearGradient>
            </defs>
            <g fill="#fff" opacity="0.9">
              <circle cx="50" cy="34" r="16" />
              <rect x="24" y="56" width="52" height="18" rx="8" />
            </g>
          </svg>
        </div>
        <div>
          <h1>Saqr Almeliki</h1>
          <p class="role">University student — IT Major</p>
        </div>

        <div class="contact">
          <div class="chip">Mobile App Developer</div>
          <div style="font-size:13px;color:var(--muted)">Location: Yemen</div>
        </div>
      </div>

      <div class="section">
        <h3>Skills</h3>
        <div class="skills">
          <div class="skill">Mobile App Development</div>
          <div class="skill">Flutter / Dart</div>
          <div class="skill">React Native</div>
          <div class="skill">UI / UX</div>
          <div class="skill">Git & GitHub</div>
          <div class="skill">Firebase</div>
        </div>
      </div>

      <div class="section">
        <h3>Education</h3>
        <div style="font-weight:600">Bachelor — Information Technology</div>
        <div style="color:var(--muted);font-size:13px;margin-top:6px">University (current student)</div>
      </div>

      <div class="section">
        <h3>Contact</h3>
        <div style="color:var(--muted);font-size:14px">Email: <span style="color:#fff;font-weight:600">your.email@example.com</span></div>
        <div style="color:var(--muted);font-size:14px;margin-top:6px">Phone: <span style="color:#fff;font-weight:600">+967 — — — —</span></div>
      </div>

    </aside>

    <main class="main">
      <div class="headline">
        <div>
          <h2 style="margin:0;font-size:22px">Profile</h2>
          <p class="about">I am a university student majoring in Information Technology with a passion for building beautiful and performant mobile applications. I focus on delivering clean UI, good user experience, and reliable app architecture.</p>
        </div>
        <div style="text-align:right">
          <div class="chip">Mobile App Developer</div>
          <div style="margin-top:8px;color:var(--muted)">Available for internships & freelance</div>
        </div>
      </div>

      <div class="row">
        <div class="col info-block">
          <h4>Experience</h4>
          <p class="muted">Student projects and freelance mobile apps — worked on UI implementation, API integration, and app publishing workflows. (Add your projects below)</p>

          <div style="margin-top:12px">
            <h4>Projects</h4>
            <div class="projects">
              <div class="project">
                <div class="meta">
                  <div style="font-weight:700">Project Name</div>
                  <div style="font-size:13px;color:var(--muted)">Short description — Mobile app (Flutter)</div>
                </div>
                <div style="font-size:13px;color:var(--muted)">2024</div>
              </div>

              <div class="project">
                <div class="meta">
                  <div style="font-weight:700">Project Name</div>
                  <div style="font-size:13px;color:var(--muted)">Short description — React Native</div>
                </div>
                <div style="font-size:13px;color:var(--muted)">2023</div>
              </div>
            </div>
          </div>
        </div>

        <div class="col info-block">
          <h4>Tools & Technologies</h4>
          <p class="muted">Flutter, Dart, React Native, Java/Kotlin (Android basics), Xcode basics, Firebase, REST APIs, Git.</p>

          <div style="margin-top:14px">
            <h4>Languages</h4>
            <p style="margin:0;color:var(--muted)">Arabic (native), English (intermediate)</p>
          </div>
        </div>
      </div>

      <div style="margin-top:18px" class="info-block">
        <h4>How to reach me</h4>
        <p style="margin:0;color:var(--muted)">Replace placeholders above with your real email and phone number. You can also include a link to your GitHub or portfolio.</p>
      </div>

      <div class="download">
        <button class="btn" id="downloadBtn">Download HTML</button>
        <button class="ghost" onclick="window.print()">Print / Save as PDF</button>
      </div>

      <footer>Designed &nbsp;•&nbsp; Clean, responsive single-file CV</footer>
    </main>
  </div>

  <script>
    // download page as HTML file
    document.getElementById('downloadBtn').addEventListener('click', function(){
      const html = '<!doctype html>' + '\n' + document.documentElement.outerHTML;
      const blob = new Blob([html], {type: 'text/html'});
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url; a.download = 'Saqr_Almeliki_CV.html'; document.body.appendChild(a); a.click(); a.remove();
      URL.revokeObjectURL(url);
    });
  </script>
</body>
</html>
